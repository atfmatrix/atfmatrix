# Sable is served as static files from this repo.
# hashRouter is on in config.json, so deep links use /#/... and need no SPA rewrite.
# Do not add /* rules here — they override static assets (including index.html).
#
# Optional identity rules are unnecessary for normal static hosting.
# Matrix discovery lives at .well-known/matrix/client (static file).
